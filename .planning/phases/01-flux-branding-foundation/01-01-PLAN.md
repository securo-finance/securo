---
phase: 01-flux-branding-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
  - frontend/src/locales/en.json
  - frontend/src/locales/pt-BR.json
  - frontend/src/lib/api.ts
  - backend/app/api/export.py
  - backend/app/api/transactions.py
  - backend/tests/test_export_api.py
autonomous: true
requirements:
  - BRND-01
  - BRND-02
user_setup: []
must_haves:
  truths:
    - Users see `Flux` instead of `Securo` in the shell title, localized shell brand string, and setup/auth welcome copy touched in this phase.
    - Downloaded backup and transaction export files use Flux-branded filenames during normal product use.
    - Visible branding remains consistent across the SPA shell surfaces changed in this plan.
  artifacts:
    - frontend/index.html
    - frontend/src/locales/en.json
    - frontend/src/locales/pt-BR.json
    - frontend/src/lib/api.ts
    - backend/app/api/export.py
    - backend/app/api/transactions.py
  key_links:
    - frontend locale app-name and welcome-copy values are consumed by app layout and auth/setup pages
    - frontend and backend export filename logic both emit Flux-branded download names
---

# Phase 1 Plan 01: Visible Flux Branding And Export Labels

## Objective

- **What:** Replace visible product-name strings and operational download filenames with Flux-branded equivalents.
- **Why:** Phase 1 succeeds only if the user encounters Flux in the app shell and during routine export or backup flows.
- **Output:** Updated browser title, localized shell/auth/setup branding, Flux-branded export filenames, and regression coverage for backup downloads.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-flux-branding-foundation/01-CONTEXT.md
@.planning/phases/01-flux-branding-foundation/01-UI-SPEC.md
@frontend/index.html
@frontend/src/locales/en.json
@frontend/src/locales/pt-BR.json
@frontend/src/lib/api.ts
@backend/app/api/export.py
@backend/app/api/transactions.py

## Tasks

<task type="code">
  <name>Update visible Flux branding in shell-facing frontend copy</name>
  <files>frontend/index.html, frontend/src/locales/en.json, frontend/src/locales/pt-BR.json</files>
  <action>Replace user-facing `Securo` branding with `Flux` in the browser title, app-name translation key, and localized welcome/setup copy that surfaces in the shell, login, register, and setup entry points. Preserve the existing visual system and translation-key structure.</action>
  <verify>rg -n "Securo|Flux" frontend/index.html frontend/src/locales/en.json frontend/src/locales/pt-BR.json</verify>
  <done>Visible shell brand strings and touched auth/setup copy use Flux consistently, with no remaining Securo references in those files.</done>
</task>

<task type="code">
  <name>Rename operational download filenames to Flux</name>
  <files>frontend/src/lib/api.ts, backend/app/api/export.py, backend/app/api/transactions.py</files>
  <action>Update the backup ZIP filename and transaction CSV filename to `flux-backup-*` and `flux-transactions-*` without changing the existing API routes. Keep the current download flow and response types intact.</action>
  <verify>rg -n "flux-backup|flux-transactions|securo-backup|transactions-" frontend/src/lib/api.ts backend/app/api/export.py backend/app/api/transactions.py</verify>
  <done>Backup and transaction export downloads emit Flux-branded filenames from both frontend and backend codepaths.</done>
</task>

<task type="code">
  <name>Refresh backup filename regression coverage</name>
  <files>backend/tests/test_export_api.py</files>
  <action>Update the backup API assertion so tests enforce the new Flux-branded backup filename convention and continue validating the download contract.</action>
  <verify>pytest backend/tests/test_export_api.py</verify>
  <done>Export API tests expect the Flux backup filename prefix and pass.</done>
</task>

## Verification

- `pytest backend/tests/test_export_api.py`
- `npm --prefix frontend run build`
- `rg -n "Securo" frontend/index.html frontend/src/locales/en.json frontend/src/locales/pt-BR.json`

## Success Criteria

- [ ] User-facing shell brand strings changed from Securo to Flux in the touched frontend surfaces.
- [ ] Backup and transaction export filenames changed to Flux-branded names.
- [ ] Regression checks pass for the modified branding and export paths.
