---
phase: 04-flat-categories-and-reader-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/categories.tsx
  - frontend/src/types/index.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/pt-BR.json
  - backend/app/schemas/budget.py
  - backend/app/services/budget_service.py
  - backend/app/api/export.py
  - backend/tests/test_budget_service.py
  - backend/tests/test_budgets_api.py
  - backend/tests/test_export_api.py
autonomous: true
requirements:
  - CAT-03
  - BUDG-05
  - MIG-02
  - MIG-03
user_setup: []
must_haves:
  truths:
    - Categories are managed in a flat list that works on mobile.
    - Budget readers use category-owned budget state without exposing group metadata.
    - Backups declare a v2 compatibility/versioning strategy for the migration window.
  artifacts:
    - frontend/src/pages/categories.tsx
    - backend/app/services/budget_service.py
    - backend/app/api/export.py
  key_links:
    - `BudgetVsActual` no longer includes `group_id` or `group_name`
    - `metadata.json` in exports declares `format_version: 2.0`
---

# Phase 4 Plan 01: Flat Category View and Reader Contract Cleanup

## Objective

- **What:** Flatten category management, simplify budget comparison readers, and version export metadata for the v2 transition.
- **Why:** Phase 4 is the point where downstream readers must align to the category-owned budget model before final legacy removal.
- **Output:** Updated flat category UI, cleaned comparison contract, and versioned backup metadata with compatibility notes.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-flat-categories-and-reader-cutover/04-CONTEXT.md
@.planning/phases/04-flat-categories-and-reader-cutover/04-UI-SPEC.md
@frontend/src/pages/categories.tsx
@backend/app/services/budget_service.py
@backend/app/api/export.py

## Tasks

<task type="code">
  <name>Flatten category management UI</name>
  <files>frontend/src/pages/categories.tsx, frontend/src/locales/en.json, frontend/src/locales/pt-BR.json</files>
  <read_first>frontend/src/pages/categories.tsx</read_first>
  <action>Remove grouped list rendering and collapse controls so categories render in one flat list while preserving the current dialog-based edit flow.</action>
  <verify>npm --prefix frontend run build</verify>
  <done>The categories page is visibly flat and remains usable on mobile screens.</done>
</task>

<task type="code">
  <name>Cut budget readers over to category-owned comparison contract</name>
  <files>backend/app/schemas/budget.py, backend/app/services/budget_service.py, frontend/src/types/index.ts, backend/tests/test_budget_service.py, backend/tests/test_budgets_api.py</files>
  <read_first>backend/app/services/budget_service.py, backend/app/schemas/budget.py</read_first>
  <action>Remove group metadata from the budget comparison reader contract and update tests so downstream consumers read category-owned values only.</action>
  <verify>docker compose run --rm backend sh -lc "pip install --no-cache-dir -e '.[dev]' >/tmp/pip-phase4.log && pytest tests/test_budget_service.py tests/test_budgets_api.py"</verify>
  <done>The comparison endpoint reports category-owned budget state without legacy group fields.</done>
</task>

<task type="code">
  <name>Version backup exports for the migration window</name>
  <files>backend/app/api/export.py, backend/tests/test_export_api.py</files>
  <read_first>backend/app/api/export.py, backend/tests/test_export_api.py</read_first>
  <action>Move export metadata to v2, preserve legacy category group data inside the archive for recoverability, and document the compatibility story in metadata.</action>
  <verify>docker compose run --rm backend sh -lc "pip install --no-cache-dir -e '.[dev]' >/tmp/pip-phase4-export.log && pytest tests/test_export_api.py"</verify>
  <done>Backups remain recoverable and explicitly describe the migration compatibility model.</done>
</task>

## Verification

- `npm --prefix frontend run build`
- `docker compose run --rm backend sh -lc "pip install --no-cache-dir -e '.[dev]' >/tmp/pip-phase4.log && pytest tests/test_categories_api.py tests/test_budget_service.py tests/test_budgets_api.py tests/test_export_api.py"`

## Success Criteria

- [ ] Category management is flat and mobile-usable.
- [ ] Budget readers no longer require legacy grouped budget ownership metadata.
- [ ] Export metadata clearly versioned the v2 transition.
