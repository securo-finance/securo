---
phase: 05-legacy-removal-and-release-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/categories.tsx
  - frontend/src/pages/budgets.tsx
  - frontend/src/lib/api.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/pt-BR.json
  - backend/app/api/budgets.py
  - backend/app/main.py
  - backend/app/core/config.py
  - backend/tests/test_budgets_api.py
  - README.md
  - CONTRIBUTING.md
  - SECURITY.md
  - install.sh
  - backend/pyproject.toml
autonomous: true
requirements:
  - BRND-03
  - MIG-04
user_setup: []
must_haves:
  truths:
    - Users no longer encounter public group-based or standalone budget editing paths.
    - Budget screens read current category-owned budget values directly.
    - User-facing release/setup text says Flux where compatibility does not require legacy identifiers.
  artifacts:
    - frontend/src/pages/budgets.tsx
    - backend/app/api/budgets.py
    - README.md
  key_links:
    - `/api/budgets` CRUD routes removed; `/api/budgets/comparison` remains as reader
    - frontend no longer imports category-group API helpers
---

# Phase 5 Plan 01: Remove Public Legacy Paths and Align Release Copy

## Objective

- **What:** Remove the final public legacy group and standalone budget paths, and finish the user-facing Flux naming cleanup.
- **Why:** The migration is complete once supported workflows no longer expose deprecated editing surfaces and release/setup text matches the new product identity.
- **Output:** Simplified frontend flows, trimmed public API surface, and updated user-facing docs/setup messaging.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-legacy-removal-and-release-cleanup/05-CONTEXT.md
@frontend/src/pages/categories.tsx
@frontend/src/pages/budgets.tsx
@frontend/src/lib/api.ts
@backend/app/api/budgets.py
@backend/app/main.py
@README.md
@install.sh

## Tasks

<task type="code">
  <name>Remove active frontend dependence on group and standalone budget paths</name>
  <files>frontend/src/pages/categories.tsx, frontend/src/pages/budgets.tsx, frontend/src/lib/api.ts, frontend/src/locales/en.json, frontend/src/locales/pt-BR.json</files>
  <read_first>frontend/src/pages/categories.tsx, frontend/src/pages/budgets.tsx, frontend/src/lib/api.ts</read_first>
  <action>Strip group selection from category management, make the budgets page read category-owned limits directly, and remove obsolete API helpers from the frontend client.</action>
  <verify>npm --prefix frontend run build</verify>
  <done>The supported frontend category and budget workflow no longer uses group or standalone budget editing paths.</done>
</task>

<task type="code">
  <name>Remove public legacy budget and group routes</name>
  <files>backend/app/api/budgets.py, backend/app/main.py, backend/tests/test_budgets_api.py</files>
  <read_first>backend/app/api/budgets.py, backend/app/main.py, backend/tests/test_budgets_api.py</read_first>
  <action>Reduce the budgets router to the comparison reader only, stop exposing category-group routes publicly, and update API tests to assert that the removed legacy endpoints return 404.</action>
  <verify>docker compose run --rm backend sh -lc "pip install --no-cache-dir -e '.[dev]' >/tmp/pip-phase5.log && pytest tests/test_categories_api.py tests/test_budget_service.py tests/test_budgets_api.py tests/test_export_api.py"</verify>
  <done>Public legacy editing paths are no longer available.</done>
</task>

<task type="code">
  <name>Align user-facing setup and release copy to Flux</name>
  <files>README.md, CONTRIBUTING.md, SECURITY.md, install.sh, backend/app/core/config.py, backend/pyproject.toml</files>
  <read_first>README.md, install.sh</read_first>
  <action>Update user-facing product and setup text to Flux while leaving repository and infrastructure identifiers intact where they still act as compatibility anchors.</action>
  <verify>rg -n "Securo|securo" README.md CONTRIBUTING.md SECURITY.md install.sh backend/app/core/config.py backend/pyproject.toml</verify>
  <done>User-facing release/setup copy uses Flux consistently within the approved compatibility boundary.</done>
</task>

## Verification

- `npm --prefix frontend run build`
- `docker compose run --rm backend sh -lc "pip install --no-cache-dir -e '.[dev]' >/tmp/pip-phase5.log && pytest tests/test_categories_api.py tests/test_budget_service.py tests/test_budgets_api.py tests/test_export_api.py"`

## Success Criteria

- [ ] Users no longer encounter deprecated group-based or standalone-budget editing paths.
- [ ] Supported budget viewing flows read category-owned budget state directly.
- [ ] Flux naming is consistent across user-facing release and setup copy where compatibility allows.
